# os
.DS_Store
Thumbs.db

# editor
.vscode/
.idea/
*.swp
*.swo
*~

# logs
*.log
npm-debug.log*

# build / cache (not used in phase 1, but reserved)
node_modules/
dist/
.cache/

# env / secrets
.env
.env.*

# the studio — local-only authoring tool, never deploys
_studio/
_drafts/

# discord setup — bot token + spec, never deploys
_discord/

# internal agent / instruction docs — local context, not site content
CLAUDE.md
AGENTS.md

# brand reference material — kept locally, not deployed publicly
Final.png
icons.png

# assistant working files
.well-known/agent-*
